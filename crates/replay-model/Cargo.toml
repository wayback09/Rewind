[package]
name = "replay-model"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Future canonical replay representation (minimal for M0)"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
